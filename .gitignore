.playwright-mcp/
_review/
*.log

# Local dev runners / env files / credentials holding secrets — never commit
backend/run-dev.sh
backend/vertex-sa.json
**/.env
**/.env.local
**/*.env.local
review-*.png
verify-*.png

# brainstorming visual companion
.superpowers/

# NAS 公网部署脚本与配置(含密钥/节点信息,不入库)
/deploy.sh
/deploy/
