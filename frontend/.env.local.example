# Base URL of the Go backend (SSE generate endpoint + health check).
# Defaults to http://localhost:8080 in lib/api.ts when unset.
# Copy this file to .env.local and override only if your backend runs elsewhere.
NEXT_PUBLIC_API=http://localhost:8080
